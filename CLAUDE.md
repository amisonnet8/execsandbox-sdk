# CLAUDE.md

このファイルは ExecSandbox SDK プロジェクトで作業する際、Claude Code が常に
踏まえるべき前提を示す。

## execsandbox-sdkとは

[ExecSandbox](https://github.com/amisonnet8/execsandbox)本体が提供するWASM
ABI（`send`/`recv`/`conn_write`/`max_frame`）の上に、各言語ネイティブな皮を
被せたSDKライブラリ集。詳細は`README.md`を参照。

対応言語はTinyGo（Go）→Rustの順で決定済み（`PLAN.md`「対応言語（決定事項）」
参照）。両方とも着手済み（TinyGo版は`go/`、Rust版は`rust/`）。現在地・進捗は
`PLAN.md`参照。

## 本体リポジトリ（execsandbox）との関係

- **ABI仕様の正は本体リポジトリにある。** 本リポジトリはABIの定義を持たず、
  参照するだけ。ABIを拡張・変更したくなった場合、**このリポジトリを起点に
  変更してはならない**。必ず本体リポジトリの仕様書（`docs/spec/
  execsandbox_spec_ja.md`§5）を先に更新してもらうよう、本体側での提案を
  仰ぐこと（詳細は`.claude/rules/abi-boundary.md`）。
- **本体のコードへ依存しない。** SDKは各言語の外部関数宣言でABIを直接叩く
  だけで、本体のGoパッケージをimportしない。この依存ゼロの構造があるからこそ
  2リポジトリに分割できている（本体側`.claude/rules/directory-structure.md`
  の「依存の向き」参照）。
- 本体とこのリポジトリは別々のツールチェーンを要求する（本体はGo、SDKは
  対応言語ごとに異なる）ため、devcontainerはリポジトリごとに個別のウィンドウ
  で開くこと。同一ウィンドウで両方を横断して作業しない。
- 本体リポジトリを手元に持っていない／参照できない場合、ABIの詳細で不明点が
  あれば、コードを推測で書かずユーザーに確認すること。

## 参照すべきファイル

- **`PLAN.md`** — 実装計画・進捗管理。セッションをまたぐたびに「現在地」を
  確認・更新すること。
- **`.claude/rules/`** — 実装中に得た知見・ルールを都度育てていく（本体
  リポジトリと同じ運用方針）。現時点では雛形のみで中身は薄い。実装が進み、
  同じ種類の判断や落とし穴に複数回遭遇したら、該当ファイルへ追記するか
  新規ファイルを作ることを都度提案すること（提案するだけで、勝手に作成・
  適用はしない）。

## 開発の進め方

実装しながらこのセクションを育てること。現時点では本体リポジトリの
`.claude/rules/testing.md`等にある「開発しながらルールを蓄積する」進め方を
踏襲する想定。

## 権限・自動化について

`.claude/settings.json`により、`git push`/`pull`/`fetch`/`clone`、
`curl`/`wget`、パッケージインストール系コマンドは実行前に確認を求める設定に
なっている（本体リポジトリと同じ方針）。確認を求められた場合、無理に実行
しようとせず、指示を仰ぐこと。

## 言語について

ドキュメント・コミットメッセージ・コメントは日本語で書く（本体リポジトリと
同じ方針）。コード内の識別子・ABI関連の外部関数宣言は英語。

**公開パッケージとしての性質に注意。** 本体はCLIツールで日本語話者の利用者を
主に想定しているが、このSDKは`pkg.go.dev`・`crates.io`等の言語別パッケージ
レジストリに公開される可能性があり、読者が本体より広い（英語話者を含む）
可能性が高い。パッケージの公開ドキュメントコメント（godoc、rustdoc等）の
言語は各言語の着手時に判断する。

- **godoc（TinyGo/Go）: 英語のみ**（2026-09-08決定）。
- **rustdoc: 英語のみ**（2026-09-08決定。godocと同じ理由——crates.io/
  docs.rsの読者も本体より広く、英語話者を含むため——で一貫させた）。
